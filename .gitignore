# This is a zero-dependency static site: index.html, analysis.js and
# tests.html are the whole app. Nothing here is built or installed, so the
# only thing worth ignoring is accidental local cruft.

# Environment files -- never commit real credentials.
.env
.env.*
!.env.example

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/
